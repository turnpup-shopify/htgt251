{%- liquid
  comment
    Picks a calculator block by matching the article metafield custom.calculator_type
    against each block's Calculator key. No match means the section does not render.
  endcomment

  assign active_key = article.metafields.custom.calculator_type.value

  if active_key == blank
    assign active_key = section.settings.fallback_key
  endif

  assign active_block = null

  for block in section.blocks
    if block.type == 'calculator' and block.settings.calculator_key == active_key
      assign active_block = block
      break
    endif
  endfor
-%}

{%- if active_block != null -%}
  {%- liquid
    assign s = active_block.settings
    assign d_top = section.settings.padding_top_desktop
    assign d_right = section.settings.padding_right_desktop
    assign d_bottom = section.settings.padding_bottom_desktop
    assign d_left = section.settings.padding_left_desktop
    assign m_top = section.settings.padding_top_mobile
    assign m_right = section.settings.padding_right_mobile
    assign m_bottom = section.settings.padding_bottom_mobile
    assign m_left = section.settings.padding_left_mobile
  -%}

  <size-calculator
    class="size-calculator"
    id="SizeCalculator-{{ section.id }}"
    data-mode="{{ s.calc_mode }}"
    data-multiplier="{{ s.multiplier }}"
    data-offset-min="{{ s.offset_min }}"
    data-offset-max="{{ s.offset_max }}"
    data-round-to="{{ s.round_to }}"
    data-input-min="{{ s.input_min }}"
    data-input-max="{{ s.input_max }}"
    data-result-prefix="{{ s.result_prefix | escape }}"
    data-result-suffix="{{ s.result_suffix | escape }}"
    data-error-empty="{{ s.error_empty | escape }}"
    data-error-range="{{ s.error_range | escape }}"
    {{ active_block.shopify_attributes }}
    style="
      --sc-max-width: {{ section.settings.max_width }}px;
      --sc-accent: {{ section.settings.accent_color }};
      --sc-surface: {{ section.settings.surface_color }};
      --sc-border: {{ section.settings.border_color }};
      --sc-radius: {{ section.settings.corner_radius }}px;
      --sc-heading-font-family: {{ section.settings.heading_font_family | default: 'inherit' }};
      --sc-heading-font-size: {{ section.settings.heading_font_size }}px;
      --sc-heading-font-weight: {{ section.settings.heading_font_weight }};
      --sc-heading-color: {{ section.settings.heading_font_color }};
      --sc-body-font-family: {{ section.settings.body_font_family | default: 'inherit' }};
      --sc-body-font-size: {{ section.settings.body_font_size }}px;
      --sc-body-font-weight: {{ section.settings.body_font_weight }};
      --sc-body-color: {{ section.settings.body_font_color }};
      --sc-result-font-family: {{ section.settings.result_font_family | default: 'inherit' }};
      --sc-result-font-size: {{ section.settings.result_font_size }}px;
      --sc-result-font-weight: {{ section.settings.result_font_weight }};
      --sc-result-color: {{ section.settings.result_font_color }};
      --sc-padding-block-start: {{ m_top }}px;
      --sc-padding-inline-end: {{ m_right }}px;
      --sc-padding-block-end: {{ m_bottom }}px;
      --sc-padding-inline-start: {{ m_left }}px;
      --sc-padding-block-start-lg: {{ d_top }}px;
      --sc-padding-inline-end-lg: {{ d_right }}px;
      --sc-padding-block-end-lg: {{ d_bottom }}px;
      --sc-padding-inline-start-lg: {{ d_left }}px;
    "
  >
    {%- if s.calc_mode == 'ranges' and s.ranges_table != blank -%}
      <script type="text/plain" data-size-calculator-ranges>{{ s.ranges_table | escape }}</script>
    {%- endif -%}

    <div class="size-calculator__inner">
      {%- if s.heading != blank -%}
        <h2 class="size-calculator__heading">{{ s.heading | escape }}</h2>
      {%- endif -%}

      {%- if s.intro != blank -%}
        <div class="size-calculator__intro">{{ s.intro }}</div>
      {%- endif -%}

      <div class="size-calculator__field">
        <label class="size-calculator__label" for="SizeCalculatorInput-{{ section.id }}">
          {{ s.input_label | escape }}
        </label>

        <div class="size-calculator__controls">
          <input
            class="size-calculator__input"
            type="number"
            inputmode="decimal"
            id="SizeCalculatorInput-{{ section.id }}"
            name="size_calculator_input"
            min="{{ s.input_min }}"
            max="{{ s.input_max }}"
            step="{{ s.input_step }}"
            placeholder="{{ s.input_placeholder | escape }}"
            aria-describedby="SizeCalculatorError-{{ section.id }}"
            data-size-calculator-input
          >
          <button class="size-calculator__button" type="button" data-size-calculator-submit>
            {{ s.button_label | escape }}
          </button>
        </div>

        <p
          class="size-calculator__error"
          id="SizeCalculatorError-{{ section.id }}"
          role="alert"
          hidden
          data-size-calculator-error
        ></p>
      </div>

      <output
        class="size-calculator__result"
        for="SizeCalculatorInput-{{ section.id }}"
        aria-live="polite"
        hidden
        data-size-calculator-result
      ></output>

      {%- if s.fallback_note != blank -%}
        <noscript>
          <div class="size-calculator__intro">{{ s.fallback_note }}</div>
        </noscript>
      {%- endif -%}
    </div>
  </size-calculator>
{%- endif -%}

{% stylesheet %}
  .size-calculator {
    display: block;
    box-sizing: border-box;
    max-width: 100%;
    padding-block-start: var(--sc-padding-block-start, 0);
    padding-block-end: var(--sc-padding-block-end, 0);
    padding-inline-start: var(--sc-padding-inline-start, 0);
    padding-inline-end: var(--sc-padding-inline-end, 0);
    overflow-wrap: break-word;

    @media screen and (min-width: 750px) {
      padding-block-start: var(--sc-padding-block-start-lg, 0);
      padding-block-end: var(--sc-padding-block-end-lg, 0);
      padding-inline-start: var(--sc-padding-inline-start-lg, 0);
      padding-inline-end: var(--sc-padding-inline-end-lg, 0);
    }
  }

  .size-calculator__inner {
    box-sizing: border-box;
    width: min(100%, var(--sc-max-width, 720px));
    margin-inline: auto;
    padding: 1.5rem;
    border: 1px solid var(--sc-border, #e5e5e5);
    border-radius: var(--sc-radius, 0);
    background: var(--sc-surface, transparent);
  }

  .size-calculator__heading {
    margin-block: 0 0.5rem;
    font-family: var(--sc-heading-font-family, inherit);
    font-size: var(--sc-heading-font-size, 26px);
    font-weight: var(--sc-heading-font-weight, 600);
    line-height: 1.2;
    color: var(--sc-heading-color, inherit);
  }

  .size-calculator__intro {
    margin-block: 0 1.25rem;
    font-family: var(--sc-body-font-family, inherit);
    font-size: var(--sc-body-font-size, 16px);
    font-weight: var(--sc-body-font-weight, 400);
    line-height: 1.5;
    color: var(--sc-body-color, inherit);

    & > :last-child {
      margin-block-end: 0;
    }
  }

  .size-calculator__label {
    display: block;
    margin-block-end: 0.5rem;
    font-family: var(--sc-body-font-family, inherit);
    font-size: var(--sc-body-font-size, 16px);
    font-weight: 600;
    color: var(--sc-body-color, inherit);
  }

  .size-calculator__controls {
    display: flex;
    flex-wrap: wrap;
    gap: 0.75rem;
  }

  .size-calculator__input {
    flex: 1 1 12rem;
    box-sizing: border-box;
    min-width: 0;
    min-height: 44px;
    padding: 0.625rem 0.875rem;
    border: 1px solid var(--sc-border, #e5e5e5);
    border-radius: var(--sc-radius, 0);
    font-family: var(--sc-body-font-family, inherit);
    font-size: 16px;
    color: var(--sc-body-color, inherit);
    background: #fff;

    &[aria-invalid='true'] {
      border-color: #b3261e;
    }

    &:focus-visible {
      outline: 2px solid var(--sc-accent, #111);
      outline-offset: 2px;
    }
  }

  .size-calculator__button {
    flex: 0 0 auto;
    min-height: 44px;
    padding-inline: 1.5rem;
    border: 1px solid var(--sc-accent, #111);
    border-radius: var(--sc-radius, 0);
    font-family: var(--sc-body-font-family, inherit);
    font-size: var(--sc-body-font-size, 16px);
    font-weight: 600;
    color: #fff;
    background: var(--sc-accent, #111);
    cursor: pointer;
    transition: opacity 0.15s ease;

    &:hover {
      opacity: 0.85;
    }

    &:focus-visible {
      outline: 2px solid var(--sc-accent, #111);
      outline-offset: 2px;
    }
  }

  .size-calculator__error {
    margin-block: 0.625rem 0;
    font-family: var(--sc-body-font-family, inherit);
    font-size: 0.875rem;
    color: #b3261e;

    &[hidden] {
      display: none;
    }
  }

  .size-calculator__result {
    display: block;
    margin-block-start: 1.25rem;
    padding-block-start: 1.25rem;
    border-block-start: 1px solid var(--sc-border, #e5e5e5);

    &[hidden] {
      display: none;
    }
  }

  .size-calculator__result-value {
    display: block;
    font-family: var(--sc-result-font-family, inherit);
    font-size: var(--sc-result-font-size, 30px);
    font-weight: var(--sc-result-font-weight, 600);
    line-height: 1.2;
    color: var(--sc-result-color, inherit);
  }

  .size-calculator__result-note {
    display: block;
    margin-block-start: 0.5rem;
    font-family: var(--sc-body-font-family, inherit);
    font-size: var(--sc-body-font-size, 16px);
    line-height: 1.5;
    color: var(--sc-body-color, inherit);
  }

  .size-calculator__result-link {
    display: inline-block;
    margin-block-start: 0.75rem;
    font-family: var(--sc-body-font-family, inherit);
    font-size: var(--sc-body-font-size, 16px);
    font-weight: 600;
    color: var(--sc-accent, #111);

    &:focus-visible {
      outline: 2px solid var(--sc-accent, #111);
      outline-offset: 2px;
    }
  }

  @media (forced-colors: active) {
    .size-calculator__input:focus-visible,
    .size-calculator__button:focus-visible {
      outline: 3px solid LinkText;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .size-calculator__button {
      transition-duration: 0.01ms;
    }
  }
{% endstylesheet %}

{% javascript %}
  class SizeCalculator extends HTMLElement {
    #input = null;
    #button = null;
    #result = null;
    #error = null;
    #ranges = [];

    connectedCallback() {
      this.#input = this.querySelector('[data-size-calculator-input]');
      this.#button = this.querySelector('[data-size-calculator-submit]');
      this.#result = this.querySelector('[data-size-calculator-result]');
      this.#error = this.querySelector('[data-size-calculator-error]');
      this.#ranges = this.#parseRanges();

      this.#button?.addEventListener('click', this.#handleSubmit);
      this.#input?.addEventListener('keydown', this.#handleKeydown);
    }

    disconnectedCallback() {
      this.#button?.removeEventListener('click', this.#handleSubmit);
      this.#input?.removeEventListener('keydown', this.#handleKeydown);
    }

    /*
      Each line of the ranges table looks like this.
      12-24 | 3 to 5 inch pull | The most common size. | /collections/pulls | Shop pulls
      Only the first two parts are required.
    */
    #parseRanges() {
      const node = this.querySelector('[data-size-calculator-ranges]');
      if (!node) return [];

      return node.textContent
        .split('\n')
        .map((line) => line.trim())
        .filter(Boolean)
        .map((line) => {
          const parts = line.split('|').map((part) => part.trim());
          const bounds = (parts[0] || '').split('-');

          return {
            min: Number(bounds[0]),
            max: Number(bounds[1]),
            result: parts[1] || '',
            note: parts[2] || '',
            link: parts[3] || '',
            linkLabel: parts[4] || ''
          };
        })
        .filter((range) => !Number.isNaN(range.min) && !Number.isNaN(range.max));
    }

    #round(value) {
      const step = Number(this.dataset.roundTo) || 1;
      return Number((Math.round(value / step) * step).toFixed(2));
    }

    #showError(message) {
      if (!this.#error) return;
      this.#error.textContent = message;
      this.#error.hidden = false;
      this.#input?.setAttribute('aria-invalid', 'true');
      if (this.#result) this.#result.hidden = true;
    }

    #clearError() {
      if (!this.#error) return;
      this.#error.hidden = true;
      this.#error.textContent = '';
      this.#input?.removeAttribute('aria-invalid');
    }

    #render(value, note, link, linkLabel) {
      if (!this.#result) return;

      const prefix = this.dataset.resultPrefix || '';
      const suffix = this.dataset.resultSuffix || '';
      const headline = [prefix, value, suffix].filter(Boolean).join(' ');

      this.#result.replaceChildren();

      const valueEl = document.createElement('span');
      valueEl.className = 'size-calculator__result-value';
      valueEl.textContent = headline;
      this.#result.append(valueEl);

      if (note) {
        const noteEl = document.createElement('span');
        noteEl.className = 'size-calculator__result-note';
        noteEl.textContent = note;
        this.#result.append(noteEl);
      }

      if (link && linkLabel) {
        const linkEl = document.createElement('a');
        linkEl.className = 'size-calculator__result-link';
        linkEl.href = link;
        linkEl.textContent = linkLabel;
        this.#result.append(linkEl);
      }

      this.#result.hidden = false;
    }

    #handleKeydown = (event) => {
      if (event.key !== 'Enter') return;
      event.preventDefault();
      this.#calculate();
    };

    #handleSubmit = () => {
      this.#calculate();
    };

    #calculate() {
      const raw = this.#input?.value ?? '';

      if (raw.trim() === '') {
        this.#showError(this.dataset.errorEmpty || 'Enter a number.');
        return;
      }

      const value = Number(raw);
      const min = Number(this.dataset.inputMin);
      const max = Number(this.dataset.inputMax);

      if (Number.isNaN(value) || value < min || value > max) {
        this.#showError(this.dataset.errorRange || 'That number is out of range.');
        return;
      }

      this.#clearError();

      if (this.dataset.mode === 'ranges') {
        const match = this.#ranges.find((range) => value >= range.min && value <= range.max);

        if (!match) {
          this.#showError(this.dataset.errorRange || 'That number is out of range.');
          return;
        }

        this.#render(match.result, match.note, match.link, match.linkLabel);
        return;
      }

      const multiplier = Number(this.dataset.multiplier) || 1;
      const low = this.#round(value * multiplier + (Number(this.dataset.offsetMin) || 0));
      const high = this.#round(value * multiplier + (Number(this.dataset.offsetMax) || 0));
      const answer = low === high ? `${low}` : `${low} to ${high}`;

      this.#render(answer, '', '', '');
    }
  }

  if (!customElements.get('size-calculator')) {
    customElements.define('size-calculator', SizeCalculator);
  }
{% endjavascript %}

{% schema %}
{
  "name": "Size calculator",
  "tag": "section",
  "class": "section-size-calculator",
  "max_blocks": 12,
  "enabled_on": {
    "templates": ["article"]
  },
  "settings": [
    {
      "type": "paragraph",
      "content": "Each block below is one calculator. The article decides which one shows. Set Calculator type on the article and match it to a Calculator key here. Articles with no Calculator type set will not show this section at all."
    },
    {
      "type": "text",
      "id": "fallback_key",
      "label": "Fallback calculator key",
      "info": "Used only when an article has no Calculator type set. Leave blank to hide the section on those articles.",
      "default": ""
    },
    {
      "type": "header",
      "content": "Layout"
    },
    {
      "type": "range",
      "id": "max_width",
      "label": "Max width",
      "min": 320,
      "max": 1200,
      "step": 20,
      "unit": "px",
      "default": 720
    },
    {
      "type": "range",
      "id": "corner_radius",
      "label": "Corner radius",
      "min": 0,
      "max": 32,
      "step": 2,
      "unit": "px",
      "default": 4
    },
    {
      "type": "color",
      "id": "accent_color",
      "label": "Button and link color",
      "default": "#111111"
    },
    {
      "type": "color",
      "id": "surface_color",
      "label": "Box background",
      "default": "#f7f6f4"
    },
    {
      "type": "color",
      "id": "border_color",
      "label": "Border color",
      "default": "#e2e0dc"
    },
    {
      "type": "header",
      "content": "Heading type"
    },
    {
      "type": "text",
      "id": "heading_font_family",
      "label": "Font family",
      "info": "Leave blank to inherit the theme font. Or enter a stack such as Georgia, serif.",
      "default": ""
    },
    {
      "type": "range",
      "id": "heading_font_size",
      "label": "Font size",
      "min": 14,
      "max": 56,
      "step": 1,
      "unit": "px",
      "default": 26
    },
    {
      "type": "select",
      "id": "heading_font_weight",
      "label": "Font weight",
      "options": [
        { "value": "300", "label": "Light" },
        { "value": "400", "label": "Regular" },
        { "value": "500", "label": "Medium" },
        { "value": "600", "label": "Semibold" },
        { "value": "700", "label": "Bold" }
      ],
      "default": "600"
    },
    {
      "type": "color",
      "id": "heading_font_color",
      "label": "Font color",
      "default": "#111111"
    },
    {
      "type": "header",
      "content": "Body type"
    },
    {
      "type": "text",
      "id": "body_font_family",
      "label": "Font family",
      "default": ""
    },
    {
      "type": "range",
      "id": "body_font_size",
      "label": "Font size",
      "min": 12,
      "max": 24,
      "step": 1,
      "unit": "px",
      "default": 16
    },
    {
      "type": "select",
      "id": "body_font_weight",
      "label": "Font weight",
      "options": [
        { "value": "300", "label": "Light" },
        { "value": "400", "label": "Regular" },
        { "value": "500", "label": "Medium" },
        { "value": "600", "label": "Semibold" },
        { "value": "700", "label": "Bold" }
      ],
      "default": "400"
    },
    {
      "type": "color",
      "id": "body_font_color",
      "label": "Font color",
      "default": "#3d3d3d"
    },
    {
      "type": "header",
      "content": "Result type"
    },
    {
      "type": "text",
      "id": "result_font_family",
      "label": "Font family",
      "default": ""
    },
    {
      "type": "range",
      "id": "result_font_size",
      "label": "Font size",
      "min": 16,
      "max": 64,
      "step": 1,
      "unit": "px",
      "default": 30
    },
    {
      "type": "select",
      "id": "result_font_weight",
      "label": "Font weight",
      "options": [
        { "value": "300", "label": "Light" },
        { "value": "400", "label": "Regular" },
        { "value": "500", "label": "Medium" },
        { "value": "600", "label": "Semibold" },
        { "value": "700", "label": "Bold" }
      ],
      "default": "600"
    },
    {
      "type": "color",
      "id": "result_font_color",
      "label": "Font color",
      "default": "#111111"
    },
    {
      "type": "header",
      "content": "Desktop padding"
    },
    {
      "type": "range",
      "id": "padding_top_desktop",
      "label": "Top",
      "min": 0,
      "max": 120,
      "step": 4,
      "unit": "px",
      "default": 40
    },
    {
      "type": "range",
      "id": "padding_right_desktop",
      "label": "Right",
      "min": 0,
      "max": 120,
      "step": 4,
      "unit": "px",
      "default": 24
    },
    {
      "type": "range",
      "id": "padding_bottom_desktop",
      "label": "Bottom",
      "min": 0,
      "max": 120,
      "step": 4,
      "unit": "px",
      "default": 40
    },
    {
      "type": "range",
      "id": "padding_left_desktop",
      "label": "Left",
      "min": 0,
      "max": 120,
      "step": 4,
      "unit": "px",
      "default": 24
    },
    {
      "type": "header",
      "content": "Mobile padding"
    },
    {
      "type": "range",
      "id": "padding_top_mobile",
      "label": "Top",
      "min": 0,
      "max": 120,
      "step": 4,
      "unit": "px",
      "default": 24
    },
    {
      "type": "range",
      "id": "padding_right_mobile",
      "label": "Right",
      "min": 0,
      "max": 120,
      "step": 4,
      "unit": "px",
      "default": 16
    },
    {
      "type": "range",
      "id": "padding_bottom_mobile",
      "label": "Bottom",
      "min": 0,
      "max": 120,
      "step": 4,
      "unit": "px",
      "default": 24
    },
    {
      "type": "range",
      "id": "padding_left_mobile",
      "label": "Left",
      "min": 0,
      "max": 120,
      "step": 4,
      "unit": "px",
      "default": 16
    }
  ],
  "blocks": [
    {
      "type": "calculator",
      "name": "Calculator",
      "settings": [
        {
          "type": "text",
          "id": "calculator_key",
          "label": "Calculator key",
          "info": "Must match the article Calculator type value exactly. Example cabinet-hardware.",
          "default": "cabinet-hardware"
        },
        {
          "type": "header",
          "content": "Content"
        },
        {
          "type": "text",
          "id": "heading",
          "label": "Heading",
          "default": "Cabinet Hardware Size Calculator"
        },
        {
          "type": "richtext",
          "id": "intro",
          "label": "Intro text",
          "default": "<p>Enter your drawer width in inches to find the pull size that fits.</p>"
        },
        {
          "type": "text",
          "id": "input_label",
          "label": "Input label",
          "default": "Drawer width in inches"
        },
        {
          "type": "text",
          "id": "input_placeholder",
          "label": "Input placeholder",
          "default": "24"
        },
        {
          "type": "text",
          "id": "button_label",
          "label": "Button label",
          "default": "Calculate"
        },
        {
          "type": "richtext",
          "id": "fallback_note",
          "label": "No JavaScript fallback",
          "default": "<p>Use the size chart above to find your pull size.</p>"
        },
        {
          "type": "header",
          "content": "Math"
        },
        {
          "type": "select",
          "id": "calc_mode",
          "label": "How the answer is worked out",
          "options": [
            { "value": "formula", "label": "Formula" },
            { "value": "ranges", "label": "Ranges table" }
          ],
          "default": "formula"
        },
        {
          "type": "number",
          "id": "multiplier",
          "label": "Multiplier",
          "visible_if": "{{ block.settings.calc_mode == 'formula' }}",
          "info": "0.33 for the one third pull rule. 1 to start from the input itself.",
          "default": 0.33
        },
        {
          "type": "number",
          "id": "offset_min",
          "label": "Low end offset",
          "visible_if": "{{ block.settings.calc_mode == 'formula' }}",
          "info": "Added to the low end. Use a negative number to subtract.",
          "default": 0
        },
        {
          "type": "number",
          "id": "offset_max",
          "label": "High end offset",
          "visible_if": "{{ block.settings.calc_mode == 'formula' }}",
          "info": "Set both offsets the same to return a single number instead of a range.",
          "default": 1
        },
        {
          "type": "select",
          "id": "round_to",
          "label": "Round the answer to",
          "visible_if": "{{ block.settings.calc_mode == 'formula' }}",
          "options": [
            { "value": "0.25", "label": "Nearest quarter inch" },
            { "value": "0.5", "label": "Nearest half inch" },
            { "value": "1", "label": "Nearest inch" },
            { "value": "2", "label": "Nearest 2 inches" }
          ],
          "default": "1"
        },
        {
          "type": "textarea",
          "id": "ranges_table",
          "label": "Ranges table",
          "visible_if": "{{ block.settings.calc_mode == 'ranges' }}",
          "info": "One line per range. Format is from-to | answer | note | link | link label. Note, link, and link label are optional.",
          "default": "0-12 | 1 to 1.5 inch knob or 3 inch pull | Good for spice pull outs and slim trash bins.\n12-24 | 3 to 5 inch pull | The most common size. Leaves clean wood on both sides.\n24-30 | 6 to 9 inch pull, or two knobs | One long pull reads modern. Two knobs read classic.\n30-42 | 10 to 12 inch pull, or two 5 inch pulls | Heavy pot drawers need this."
        },
        {
          "type": "header",
          "content": "Answer wording"
        },
        {
          "type": "text",
          "id": "result_prefix",
          "label": "Answer prefix",
          "default": "Use a"
        },
        {
          "type": "text",
          "id": "result_suffix",
          "label": "Answer suffix",
          "default": "inch pull"
        },
        {
          "type": "header",
          "content": "Input limits"
        },
        {
          "type": "number",
          "id": "input_min",
          "label": "Lowest number allowed",
          "default": 6
        },
        {
          "type": "number",
          "id": "input_max",
          "label": "Highest number allowed",
          "default": 48
        },
        {
          "type": "number",
          "id": "input_step",
          "label": "Step",
          "default": 1
        },
        {
          "type": "text",
          "id": "error_empty",
          "label": "Empty input message",
          "default": "Enter a width in inches."
        },
        {
          "type": "text",
          "id": "error_range",
          "label": "Out of range message",
          "default": "Enter a width between 6 and 48 inches."
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Size calculator",
      "blocks": [
        {
          "type": "calculator",
          "settings": {
            "calculator_key": "cabinet-hardware",
            "heading": "Cabinet Hardware Size Calculator",
            "intro": "<p>Enter your drawer width in inches to find the pull size that fits.</p>",
            "input_label": "Drawer width in inches",
            "input_placeholder": "24",
            "calc_mode": "formula",
            "multiplier": 0.33,
            "offset_min": 0,
            "offset_max": 1,
            "round_to": "1",
            "result_prefix": "Use a",
            "result_suffix": "inch pull",
            "input_min": 6,
            "input_max": 48,
            "error_range": "Enter a width between 6 and 48 inches.",
            "fallback_note": "<p>Use the size chart above to find your pull size.</p>"
          }
        },
        {
          "type": "calculator",
          "settings": {
            "calculator_key": "vanity-mirror",
            "heading": "Vanity Mirror Size Calculator",
            "intro": "<p>Enter your vanity width in inches to find the mirror width that fits.</p>",
            "input_label": "Vanity width in inches",
            "input_placeholder": "36",
            "calc_mode": "formula",
            "multiplier": 1,
            "offset_min": -6,
            "offset_max": -4,
            "round_to": "1",
            "result_prefix": "Look for a",
            "result_suffix": "inch wide mirror",
            "input_min": 18,
            "input_max": 96,
            "error_range": "Enter a width between 18 and 96 inches.",
            "fallback_note": "<p>Use the size chart above to find your mirror width.</p>"
          }
        }
      ]
    }
  ]
}
{% endschema %}
